# TerraformModule to create dev and prod environments
Instance creation depending on simple variables
```
Run terraform apply from "dev" folder gives you everything with "dev" tags and vice versa for "prod"
```
